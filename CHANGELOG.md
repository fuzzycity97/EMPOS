# Changelog

All notable changes to the EMPOS (Enterprise Multi-Industry Point of Sale & Clinical Operating System) project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## [1.1.0] - Unreleased

### Added
- **Multi-Specialty Medical Clinic Store Builder & Turnkey Blueprints (`StoreBuilderWizardPage`, `SpecificIndustry`, `StoreBlueprintModel`)**:
  - Expanded `SpecificIndustry` to include all medical clinical disciplines across all 7 major clinical groups: `ophthalmologyClinic`, `orthopedicClinic`, `physiotherapyClinic`, `gastroClinic`, `cardiologyClinic`, `dermatologyClinic`, `entClinic`, `neurologyClinic`, `neuroOtologyClinic`, `neuroPsychiatryClinic`, `rhinologySinusClinic`, `dentalClinic`, `vascularVeinClinic`, `pulmonologyClinic`, `endocrinologyClinic`, `urologyClinic`, `obgynClinic`, `podiatryClinic`, `plasticSurgeryClinic`, `medicalAestheticsClinic`, `painManagementClinic`, `acupunctureClinic`, `speechPathologyClinic`, and `pediatricClinic`.
  - Built dedicated turnkey blueprints (`defaultOphthalmologyBlueprint()`, `defaultOrthopedicsBlueprint()`, `defaultPhysiotherapyBlueprint()`, `defaultGastroenterologyBlueprint()`, `defaultCardiologyBlueprint()`, `defaultDermatologyBlueprint()`, `defaultDentalBlueprint()`, `defaultClinicBlueprint()`).
  - Ensured every medical clinic provisioned through Store Builder automatically activates the Doctor Station (`sw.clinic_doctor_station: true`) and Reception Desk (`sw.clinic_reception: true`) with front-desk retail POS support (`sw.retail_pos: true`), preventing any fallback to retail templates.
- **Universal Multi-Specialty 3D Anatomical Canvas across 7 Major Groups & 22 Disciplines (`MultiSpecialtyAnatomyCanvasWidget`, `DoctorStationPage`)**:
  - Engineered a reactive multi-specialty 3D anatomical viewer that dynamically adapts to each medical clinic's profession:
    - **1. Head, Brain & Neurological Specialties**:
      - *Neurology & Neurosurgery*: 3D Brain & Skull Base visualizer with 5 layer filters (`Cortical Lobes`, `Ventricles & CSF`, `Deep Basal Ganglia`, `Cranial Nerves I–XII`, `Circle of Willis`) and procedural actions (Ischemic/Hemorrhagic Stroke Localization, Glioma/Meningioma Tumor Mapping, Aneurysm Coiling, DBS Target Trajectory, Craniotomy Flap Planning).
      - *Neuro-Otology & Balance Clinics*: 3D Vestibular Labyrinth & Temporal Bone visualizer with 4 layer filters (`Cochlea`, `Semicircular Canals`, `Otolith Organs`, `Vestibulocochlear Nerve CN VIII`) and actions (BPPV Otolith Canalithiasis Displacement Tracking, Epley Particle Repositioning Maneuver, Cochlear Implant Array Fitting, Superior Canal Dehiscence).
      - *Neuro-Psychiatry & TMS Clinics*: 3D Functional Brain Network visualizer with 3 layer filters (`Prefrontal Cortex`, `Limbic System`, `Default Mode Network`) and actions (rTMS Coil Target Positioning, ECT Vector Mapping, Deep TMS for Refractory OCD, fMRI Biomarker Tracking).
    - **2. Eye, Face, ENT & Dental Clinics**:
      - *Ophthalmology*: 3D Ocular Globe visualizer with 4 layer filters (`Fibrous Cornea/Sclera`, `Vascular Uvea`, `Sensory Retina`, `Optical Media`) and actions (LASIK Flap Vectors, Cataract Toric IOL Alignment, Retinal Detachment Tear Mapping, Glaucoma SLT Laser).
      - *Rhinology & Sinus Clinics (ENT)*: 3D Paranasal Sinus visualizer with 4 layer filters (`Nasal Septum`, `Turbinates`, `Paranasal Sinuses`, `Nasopharynx`) and actions (FESS Path Mapping, Balloon Sinuplasty Dilator Vector, Septoplasty Deviation Correction, Nasal Polyp Resection).
      - *Dental & Oral Surgery*: 3D CBCT / FDI Odontogram with 32 adult and 20 deciduous teeth, enamel/dentin/pulp, MB2 root canal navigation, implant bone density heatmaps, and apicoectomy angles.
    - **3. Cardiovascular, Thoracic & Vein Clinics**:
      - *Cardiology*: 3D Heart & Coronary Tree visualizer with 4 layer filters (`Myocardium`, `Coronary Arteries LAD/LCx/RCA`, `Heart Valves`, `Conduction SA/AV Nodes`) and actions (Stent Expansion Sizing, TAVR Landing Zones, AFib Ablation Vectors, FFR Pressure Mapping).
      - *Vein & Vascular Clinics (Phlebology)*: Peripheral Angiogram & Venous Network visualizer with 4 layer filters (`Arterial Tree`, `Venous Network`, `Deep Vein Valves`, `Vessel Wall Cross-Section`) and actions (EVLA Laser Ablation, Sclerotherapy Injection Points, DVT Clot Tracking, Carotid Stent Landing Zones).
      - *Pulmonology & Respiratory*: 3D Tracheobronchial Tree & Lung Parenchyma visualizer with 4 layer filters (`Airways & Bronchi`, `Lung Segments`, `Pulmonary Vasculature`, `Pleural Space`) and actions (EBUS-TBNA Biopsy Vectors, Asthma/COPD Airway Remodeling Simulation, Chest Tube Trajectory, Bronchial Stent Placement).
      - *Endocrinology & Glands*: 3D Glandular visualizer with 4 layer filters (`Thyroid Lobes`, `Parathyroid Glands`, `Adrenal Cortex/Medulla`, `Pituitary`) and actions (TIRADS Nodule Scoring, Parathyroid Adenoma Mapping, Adrenal Lesion Depth).
    - **4. Abdominal, Pelvic & Endocrine Clinics**:
      - *Gastroenterology & Endoscopy*: 3D Digestive & HPB visualizer with 4 layer filters (`Mucosa/Submucosa`, `Muscularis Externa`, `Hepatobiliary Tree`, `Pancreatic Ducts`) and actions (Colonoscopy Polyp Localization, EMR Margins, Peptic Ulcer Cautery Zones, Biliary Stents).
      - *Urology & Men's Health*: 3D Genitourinary Viscera visualizer with 4 layer filters (`Renal Cortex/Pelvis`, `Ureters`, `Bladder`, `Prostate Peripheral vs. Transition Zones`) and actions (Kidney Stone Holmium Laser Lithotripsy, MRI-TRUS Fusion Prostate Biopsy, BPH HoLEP Enucleation, Varicocele Ligation).
      - *OB/GYN & Fertility (REI)*: 3D Pelvic Organs & Fetal visualizer with 4 layer filters (`Myometrium/Endometrium`, `Ovaries & Antral Follicles`, `Fallopian Tubes`, `Fetal Trimesters`) and actions (Fibroid FIGO Grading, Endometriosis Lesion Marking, IUI/IVF Catheter Vector, IUD Placement).
    - **5. Musculoskeletal, Sports & Physical Rehab**:
      - *Orthopedic Sports & Trauma*: 3D Skeletal Bone Explorer with 4 layer filters (`Cortical/Cancellous Bone`, `Articular Cartilage`, `Joint Capsules`, `Ligaments & Tendons`) and actions (ORIF Fracture Reduction Vectors, ACL Reconstruction Tunnel Alignment, Rotator Cuff Anchor Placement, THA/TKA Arthroplasty Alignment).
      - *Physiotherapy & Kinesiology*: 3D Muscular & Movement System with 4 layer filters (`Superficial & Deep Muscles`, `Myofascial Chains`, `Vertebrae & Discs`, `Peripheral Nerves`) and actions (Trigger Point Dry Needling, Muscle Strain Grading, Spinal Subluxation Force Vectors, ROM Arcs).
      - *Podiatry & Orthotics (P&O)*: 3D Foot & Biomechanics visualizer with 4 layer filters (`Tarsal/Metatarsal Bones`, `Plantar Fascia`, `Arch Tendons`, `Plantar Pressure Distribution`) and actions (Custom Orthotic Impression Fitting, Plantar Fasciitis Injection Site, Bunion Osteotomy Angle).
    - **6. Plastic Surgery, Aesthetics & Dermatology**:
      - *Cosmetic Plastic Surgery*: 3D Craniofacial & Body Soft Tissue visualizer with 4 layer filters (`Epidermis/Dermis`, `Fat Compartments`, `SMAS Layer`, `Facial Skeleton`) and actions (Rhinoplasty Cartilage Graft Simulation, Facelift SMAS Tension Vectors, Breast Implant Pocket Simulation, Liposuction Cannula Paths).
      - *Medical Aesthetics (Injectors)*: 3D Facial Neurovascular Danger Zones with 4 layer filters (`Facial/Angular Arteries`, `Motor/Sensory Nerves`, `Fat Pads`, `SMAS Danger Planes`) and actions (Botox Unit Injection Point Mapping, Dermal Filler HA Depth Vectors, Thread Lift Tension Lines, Vascular Occlusion Highlighting).
      - *Dermatology & Hair Restoration*: 3D Cutaneous & Scalp visualizer with 4 layer filters (`Epidermis`, `Dermis`, `Hair Follicles`, `Micro-Vascular Plexus`) and actions (Whole-Body Mole Mapping, Mohs Micrographic Surgery Margins, FUE Hair Graft Density & Angle Mapping).
    - **7. Interventional Pain, Anesthesia & Allied Specialties**:
      - *Interventional Pain Care*: 3D Neuro-Spinal visualizer with 4 layer filters (`Vertebrae & Facets`, `Epidural Space`, `Dorsal Root Ganglia`, `Sympathetic Chains`) and actions (Fluoroscopy-Guided Epidural Needle Trajectory, RFA Burn Radius, Spinal Cord Stimulator Lead Targets).
      - *Acupuncture & Eastern Medicine*: 3D Anatomical Meridian & Acupoint visualizer with 3 layer filters (`Meridians & Points`, `Musculoskeletal Base`, `Neurovascular Trunks`) and actions (Needle Insertion Depth/Angle Safety Checks, Pneumothorax Avoidance Zones, Trigger Point vs Acupoint Mapping).
      - *Speech-Language Pathology (SLP)*: 3D Swallowing & Vocal Apparatus visualizer with 4 layer filters (`Tongue Musculature`, `Palate`, `Epiglottis`, `Vocal Cords`) and actions (Dysphagia Aspiration Trajectory Visualization, Vocal Cord Nodule Contact Zone Analysis, Palatal Elevation Simulation).
- **Searchable Clinical Status Catalog & 4D/3D Inspector Modal (`ClinicalStatusCatalog`, `ClinicalStatusInspectorModal`, `ClinicalAnatomyStatusEntry`)**:
  - Developed a comprehensive master library of clinical statuses with ICD-10 diagnostic codes, Arabic & English titles, pathology categories, severity levels, clinical descriptions, and billing procedures across all 22 medical specialties.
  - **Live Search & Category Filtering Engine**: Real-time query text search across titles, ICD-10 codes, descriptions, and procedure names. Real-time category chips (`All`, `Healthy`, `Trauma`, `Inflammation`, `Degenerative`, `Vascular`, `Surgical`, `Neoplasm`, `Neoplastic`, `Structural`, `Functional`, `Obstruction`, `Fluid`, `Hypertrophy`, `Procedural`, `Ulcerative`, `Intractable`).
  - **Comprehensive 4D/3D Canvas Visualization**:
    - Tapping any organ or hotspot displays glowing halo borders and active status pill badges.
    - **Visual Active Statuses & Diagnostic Log Tray**: Renders below the 3D canvas showing all active diagnoses, ICD-10 codes, procedure names, fees in EGP, with 1-click edit (re-opening search inspector) and clear actions.
    - Integrated with Dental `ToothEditorSheet` via a prominent search button.
- **Smart Role-Based Station Toggles & Scoping (`MainShell`)**:
- **Smart Role-Based Station Toggles & Scoping (`MainShell`)**:
  - Granted `UserRole.receptionist` access to `POS Cashier` (`PosPage`) and `Orders & Returns` (`OrdersHistoryPage`) when `sw.retail_pos` is enabled. Reception desks in dental clinics, polyclinics, and wellness centers can now sell OTC products (dental hygiene kits, medicated mouthwashes, skincare, whitening gels) and process patient copayments and returns.
  - Doctor station options (such as the 3D dental odontogram, tooth chart editor, and clinical triage) remain strictly isolated from receptionists, preventing clinical tampering.
  - Granted `UserRole.manager` full 360Â° access across all navigation modules, including the Doctor Station (`DoctorStationPage`), enabling complete floor oversight, clinical audits, and financial reconciliation.
- **Doctor Station Consumables & Stock Monitor (`DoctorStationPage`)**:
  - Added a dedicated `[Ù…Ø®Ø²ÙˆÙ† Ø§Ù„Ù…Ø³ØªÙ‡Ù„ÙƒØ§Øª ÙˆØ§Ù„Ù…ÙˆØ§Ø¯ (Stock)]` button in the doctor queue sidebar header.
  - Implemented `_showDoctorConsumablesStockDialog` allowing doctors to view real-time quantities of all tracked supplies (e.g., Sterile Medical Gloves Box, Anesthesia carpules, Composite syringes, Sutures, Needles) with stock counts, status indicators (In Stock, Low Stock, Out of Stock), barcode badges, and instant search.
- **Runtime Feature Toggles Direct Authority (`StoreBlueprint.isEnabled`, `MainShell`)**: Every feature toggle (`sw.*` and `hw.*`) is strictly authoritative over hardcoded industry defaults. Explicit false entries disable features across navigation tabs, search headers, checkout options, and CRM views.
- **Immediate Local Toggle Persistence (`TechnicianFleetConsolePage`, `ConfigBloc`)**: Toggling switches in the technician console immediately dispatches `UpdateBlueprintEvent` to local `ConfigBloc`, persisting to Hive and updating the running application in 0ms without requiring deployment.
- **Dynamic Station Role Broadcasts (`LanSyncRepositoryImpl`, `AuthBloc`)**: Added `updateStationIdentity()` to broadcast `nodeJoined` sync envelopes upon user login, instantly announcing active workstation roles (`Doctor Station`, `Reception Desk`, `POS Cashier`) across the LAN mesh without requiring app restarts.
- **Quick Station Profile Switcher (`MainShell`)**: Added a convenient station profile switcher menu in the application header bar next to the lock icon (`Receptionist 3333`, `Doctor 1111`, `Admin 0000`) for rapid multi-station workflow testing on a single development machine.
- **Interactive 3D Odontogram Exploration Mode (`DoctorStationPage`)**: Rendered the complete 32 adult / 20 pediatric 3D dental matrix canvas immediately in sandbox exploration mode when no active patient is selected, with a 1-click test patient check-in button.
- **Doctor Rosters & Shifts Admin/Manager Switchboard (`DoctorRoster`, `DoctorRosterModel`, `DoctorRosterManagerDialog`)**: Enterprise doctor shifts, working days (Sun-Sat), operating hours, room/suite assignments, and slot durations configuration module restricted to Admins and Managers. Persisted in Hive (`empos_doctor_rosters_box`) and broadcast in real time across LAN peer stations via `clinic.rosters_updated`.
- **Clinical Fast-Booking & 1-Click Lobby Check-In Engine (`ClinicalBookingDialog`, `AppointmentActionsDialog`)**: Patient intake booking dialog with CRM phone/name autocomplete, active doctor roster selection, procedure dropdown, and smart slot reservation. Added 1-click `[دخول صالة الانتظار (Check In)]` action directly dispatching `CheckInPatientEvent` into `ClinicBloc`.
- **Doctor Station "My Appointments Today" (`DoctorStationPage`)**: Dedicated modal in the doctor queue header displaying all appointments scheduled for the logged-in doctor for the active day with patient name, procedure, time, and check-in status.
- **Dynamic Account Ledger & History Settlement Linkage (`DoctorStationPage`, `HistoricalVisitDetailsDialog`)**: Bound historical consultation visits directly to real-time `Customer.totalDebt` from `CustomerBloc`. Synchronizes partial debt settlements and cash settlements directly from the customer ledger, displaying a live balance badge and `[View Account Ledger]` button, and dynamically marking visits as `PAID & SETTLED (Account Cleared)` when customer debts are cleared.
- **Live Loading Progress Bar for Test Execution (`run_tests.ps1`)**: Built interactive test runner script with dynamic test discovery (`\b(test|testWidgets)\s*\(`) that calculates total tests in target files and renders a live, real-time loading bar (`[############------------] 50% (2/4) [00:01] <test_name>`) alongside PowerShell `Write-Progress` with exact percentage, completion counts, elapsed timer, and immediate failure highlighting in red.
- **Mandatory AI Agent Operating Rules (`AGENTS.md`, `GEMINI.md`, `.agents/rules/testing_progress_bar.md`)**: Codified the live loading progress bar rule across root agent instructions and Antigravity rule discovery paths, requiring all AI agents and developers to execute tests via `.\run_tests.ps1 <target>` rather than bare `flutter test`.
- **Host Role Clearance & Client Reversion (`LanSyncRepositoryImpl`, `SyncConnectionManager`)**: Added `clearPersistedRole: true` to `LanSyncRepositoryImpl.disconnect()` to purge persisted server roles from `SharedPreferences`, and added `clearHostMode()` to `SyncConnectionManager` to gracefully revert from host mode without lingering server daemon bindings.

### Changed
- **Fleet-Wide Master Blueprint Remote Deployment (`TechnicianFleetConsolePage`, `ConfigBloc`)**: Deploys configurations with `targetStationId: 'all'` so all connected client stations ingest and persist the Master Blueprint into local Hive storage.
- **Dental Tooth Chart Editor Default in Clinic Blueprint (`StoreBlueprintModel`)**: Added `'sw.dental_tooth_chart_editor': true` to `StoreBlueprintModel.defaultClinicBlueprint()`.
- **Odontogram Queued Patient Feature Toggle Binding (`DoctorStationPage`)**: Bound tooth chart initialization, queue clicks, and active patient consultation to `currentBlueprint.isEnabled('sw.dental_tooth_chart_editor')` alongside `blueprint.isDental`. Added `_getEffectiveToothChart` guaranteeing full 32 adult or 20 primary teeth are rendered without empty list glitches.
- **LAN Sync Hub Client Node Filtering & Stale Socket Eviction (`LanSyncDialog`, `LanSyncRepositoryImpl`)**: De-duplicated stations by unique node ID, evicted stale channels upon node reconnect, and filtered local host/god-mode-hub out of the connected clients list.
- **God Mode Master Hub Identity Refactor & Provisioning Authority (`TechnicianFleetConsolePage`)**: Decoupled the Technician Fleet & Provisioning Hub from client workstation identities (`receptionist`). The console now identifies as the centralized master blueprint template authority (`id: 'god-mode-hub'`, `role: 'Master Blueprint (God Hub)'`, `appName: 'EMPOS Technician Provisioning Hub'`, badge `GOD MASTER`) that gives configurations to connected client workstations (Doctor, Reception, POS) rather than acting as a configurable satellite client.
- **Global Fleet Template vs. God Hub Customization Disambiguation (`TechnicianFleetConsolePage`)**: Clearly separated the `Global Fleet Blueprint Template` (broadcast to remote client workstations) from the God Hub authority. Added informative banners clarifying that the God Mode hub does not consume operational workstation customizations itself.
- **Client Mode Toolbar Controls in God Hub (`TechnicianFleetConsolePage`)**: Added dedicated Server IP input and `Connect as Client` controls directly to the Technician Console toolbar so the station can connect as a client to any existing network server daemon without forced host binding.
- **Role & App Name Inference Extension (`ConnectedNode`, `LanSyncRepositoryImpl`)**: Extended `ConnectedNode.inferAppName` and `LanSyncRepositoryImpl.getLocalStationRole` to recognize `god`, `admin`, and `tech` roles.
- **Multi-Platform Responsive Calendar Grid & Header Layout (`UniversalCalendarGridWidget`, `BookingsCalendarPage`)**: Eliminated `RenderFlex overflow` using `LayoutBuilder` breakpoints and rebuilt `UniversalCalendarGridWidget` with 2D synchronized scrolling, sticky doctor column headers with active status indicators, off-duty slot hatching, and multi-platform support across mobile, tablet, desktop, and web.
- **RBAC Station Privacy Guard (`MainShell`)**: Removed the universal "Schedule & Bookings" navigation tab from the `doctor` role, ensuring doctors focus solely on their queue and consultation workbench while receptionists manage bookings and intake.
- **Central Patient Medical Archive & History Search (`DoctorStationPage`)**: Added global patient archive search dialog accessible directly from the doctor's queue sidebar header. Doctors can search any registered patient by full name or phone number at any time to review their complete clinical history, past visits, and odontograms, or immediately select them in the queue.
- **Dynamic Store Business Name Branding (`MainShell`)**: Wired `blueprint.storeName` directly into the application header bar (with fallback to 'EMPOS™ Enterprise'), ensuring customized business names configured during Store Builder and Blueprint setup are prominently displayed.
- **Real Laboratory Panels & Bloodwork Tracking (`ClinicVisit`, `ClinicVisitModel`, `DoctorStationPage`, `HistoricalVisitDetailsDialog`)**: Replaced dummy simulated bloodwork tables with real clinical data input. Doctors can record real bloodwork/lab results (CBC, BMP, pathology) in `DoctorStationPage`, which are persisted to `ClinicVisit.labResults` and faithfully displayed in `HistoricalVisitDetailsDialog`.
- **Real Image Attachments & Radiograph Lightbox Persistence (`DoctorAttachmentsLightbox`, `DoctorStationPage`, `HistoricalVisitDetailsDialog`)**: Attached actual image file paths (`filePath`) and titles into `ClinicVisit.attachmentPaths` and `attachmentTitles`. Replaced placeholder silhouettes with real disk-backed images in thumbnails and full-screen inspection modals via `Image.file`.
- **Reception LAN Server Disconnection Banner (`ClinicReceptionPage`)**: Added immediate offline status alert banner to the Receptionist dashboard with real-time connection monitoring, auto-reconnect trigger, and Network Hub dialog access.
- **Dynamic Medical Risk Factors & Admin Switchboard (`MedicalRiskFactor`, `MedicalRiskFactorManagerDialog`, `ClinicBloc`)**: Enterprise clinical risk indicators framework allowing Admins and Clinic Managers to dynamically add new risk factors, toggle active statuses, assign custom hex colors from a 10-color clinical palette, and select custom Lucide vector icons without hardcoded modifications. Changes persist in Hive (`empos_clinic_risk_factors_box`) and propagate in real time across LAN peer stations via `clinic.risk_factors_updated`.

### Fixed
- **Feature Toggles Ignored or Overridden by Industry Booleans (`pos_search_header.dart`, `main_shell.dart`, `checkout_dialog.dart`, `customers_page.dart`, `doctor_station_page.dart`)**: Replaced hardcoded vertical `||` checks with `bp.isEnabled(...)` calls so toggles set to `false` actually turn off features (Rx scanning, table management, electronic scale, customer debt tracking, loyalty points, dental tooth chart).
- **Checkout Dialog Horizontal RenderFlex Overflow**: Wrapped status and change due rows in `Expanded` and `Flexible` with explicit width bounds, eliminating overflow assertions on compact screen widths.
- **Duplicate Local Machine & Host Daemon in Discovered Fleet (`TechnicianFleetConsolePage`)**: Filtered out local station instances and host daemons from the remote client list. When no external client workstations are active, `DISCOVERED FLEET` accurately reflects `(0)`, resolving duplicate device display (`God Hub` + `Reception Desk`).
- **Persistent Role Purging on Disconnect (`LanSyncBloc`)**: Updated `LanSyncBloc._onDisconnect` to invoke `lanSyncRepository.disconnect(clearPersistedRole: true)` to guarantee clean role clearance when stopping host mode.
- **Duplicate Patient Registration Prevention (`PatientIntakeDialog`)**: Added real-time phone number duplication validation in receptionist intake. Renders an inline warning banner (`Already registered to [Name]`) with a 1-tap `[Select]` action, blocking duplicate profile creation in CRM and clinic storage.
- **Receptionist RBAC Guard on Medical Risk Factors (`PatientIntakeDialog`)**: Removed the "Manage Factors" administrative button from the Receptionist intake modal, ensuring only Admins and Clinic Managers can create, delete, or recolor clinical risk indicators.
- **Visit History Debt Calculation & Accurate Badging (`HistoricalVisitDetailsDialog`)**: Corrected misleading `PAID & SETTLED` badges on partial payments in visit history. Dynamically computes outstanding balance ($\text{Due} = \text{Fee} - \text{Copay} - \text{Insurance}$) and badges visits accurately as `PARTIAL DEBT (Due: X EGP)` (amber), `UNPAID` (red), or `PAID & SETTLED` (green).
- **Global Unicode Mojibake & Corrupted Glyphs Scrub (`DoctorStationPage`, `ClinicReceptionPage`)**: Scrubbed corrupted `âš ï¸ ` and `â€”` mojibake artifacts from connection status banners, standardizing on clean vector `LucideIcons`.
- **Restaurant Visual Table Map (`RestaurantTableMapWidget`)**: Interactive 2D draggable and zoomable floor plan built with `InteractiveViewer` and `GestureDetector`. Allows restaurant cashiers and servers to visually view dining areas, drag and arrange table configurations, inspect table occupancy statuses (Available, Occupied, Billed, Reserved), and seamlessly park or resume dine-in tabs.
- **Kitchen Display System (`KitchenDisplaySystemPage`)**: Kanban-style horizontal order rail for the `kitchen` role. Displays active tickets across prep stages (New, Cooking, Ready, Served), with live elapsed cook timers, priority badges, item modifiers, and server name tags.
- **Doctor Station Attachments & Radiograph Lightbox (`DoctorAttachmentsLightbox`)**: Medical document and imaging dock embedded in the Doctor Station. Enables uploading and viewing patient attachments (X-Rays, Ultrasound scans, Lab reports, DICOM radiograph placeholders) with interactive zoom, rotate, and full-screen lightbox inspection.
- **LAN Sync Full Data Payload Synchronization**: Upgraded distributed synchronization across isolated Hive multi-instance databases (`INSTANCE_ID=1` and `INSTANCE_ID=2`). `CheckInPatientEvent` and `CompleteVisitEvent` now broadcast complete JSON payloads of `PatientProfile` and `ClinicVisit`. Receiving peer stations automatically extract payloads and explicitly insert them into their local databases before dispatching queue refreshes.
- **Connection Recovery Auto-Sync (State Reconciliation)**: Implemented automatic handshake state reconciliation upon connection or network drop recovery. When a client node connects to the host, it transmits `sync.request_active_state`. The host station intercepts the request, aggregates all active clinic visits (`waiting` and `inExamination`) and associated patients, and broadcasts `sync.full_state_response`. The client automatically ingests and batch-upserts the entities into its local Hive database before dispatching a queue refresh, guaranteeing zero UI desync even after network dropouts or late joins.

### Fixed
- **Doctor Station Patient History Debt Calculation & Status Badging (`DoctorStationPage`)**: Corrected patient history balance computation. Total uncollected debt is now calculated directly as $\text{Total Fees} - \text{Total Copays Paid}$, and individual visits are accurately badged as `PAID & SETTLED` (green), `PARTIAL DEBT` (amber), or `UNPAID` (red). Doctor IDs are translated to human-readable names and fees are displayed in `EGP`.
- **Customer List Reverse-Chronological Activity Sorting (`CustomerRepositoryImpl` & `CustomerLocalDataSourceImpl`)**: Sorted customer directory by most recent transaction/ledger timestamp or registration date ($t_{\text{newest}} \rightarrow t_{\text{oldest}}$) so recently examined or billed patients immediately appear at index 0.
- **Unified Risk Indicators Across Reception & Doctor (`PatientIntakeDialog` & `PatientMedicalHistoryDialog`)**: Replaced static, mismatched risk chips with dynamic chips driven by `MedicalRiskFactor`, ensuring identical clinical indicators appear at both Reception check-in and Doctor consultation.
- **Global Unicode Mojibake Cleanup**: Replaced all remaining instances of `â€¢` with clean `•` and `EÂ£` with `EGP` across presentation widgets.
- **Customer Account Ledger & Partial Payment LAN Sync (`CustomerRepository`, `CustomerBloc`, `ClinicBloc`)**: Resolved financial desync where doctor and reception stations differed on outstanding patient debts. Implemented `saveLedgerEntry` / `saveLedgerEntries` in `CustomerRepositoryImpl` to dynamically recalculate and persist net balances ($\text{Outstanding Debt} = \sum \text{Charges} - \sum \text{Payments}$) upon receiving sync broadcasts. Complete transaction audit logs are transmitted in `customer.updated` and `sync.full_state_response` so both Doctor and Reception stations reflect identical debt and full payment histories in real time.
- **Patient Intake Medical Risk History & Age Persistence (`ClinicBloc` & `PatientIntakeDialog`)**: Fixed issue where patient age, chronic conditions (Smoking, Diabetes, Hypertension), and drug allergies registered at reception were lost or failed to show at Doctor Station. Persisted effective age and risk factors to `PatientProfile` and CRM `Customer`. Selecting returning patients now automatically restores their registered age and clinical risk flags.
- **Odontogram Session Isolation (`DentalRepositoryImpl`, `ClinicBloc`, `DoctorStationPage`)**: Removed historical visit replay from `DentalRepositoryImpl.getPatientToothChart`. Fresh consultation sessions start with a clean baseline (adult or pediatric) instead of inheriting past decayed/treated teeth, while prior session tooth charts remain fully accessible in the longitudinal Patient History modal (`HistoricalVisitDetailsDialog`). Added `ResetToothChartEvent` to reset active odontogram upon completing consultations.
- **Unicode Mojibake & Missing Font Glyph Resolution (`PatientIntakeDialog`, `PatientMedicalHistoryDialog`, `DentalTooth3dCanvasWidget`, `ToothEditorSheet`)**: Cleaned corrupted mojibake emoji strings and replaced missing Material Icon square box (`[ ]`) glyphs on Windows with high-fidelity `LucideIcons` vectors.
- **Clinic Consumables Auto-Deduction (`CompleteVisitUseCase`)**: Fixed runtime type inference error during consumable inventory deduction where querying `allProducts` failed `orElse` fallback typing. Explicitly typed `allProducts` as `List<Product>` ensuring seamless auto-deductions.
- **LAN Sync State Reconciliation (`LanSyncRepositoryImpl` & `ClinicBloc`)**: Fixed LAN Sync Race Condition and Silent Parsing Error on Reconnect. Added an 800ms handshake delay in `connectToHost` to ensure the Host socket registry is fully established before requesting active state. Ensured safe JSON serialization of all registered patients on the Host and wrapped Client ingestion in strict try/catch mapping with awaited Hive batch-upserts before triggering queue reloads.
- **LAN Sync Intermediate Status Broadcast & Conflict Resolution (`ClinicBloc` & `MessageRoutes`)**: Fixed Stale Data Overwrite and added `visit.updated` broadcasting. Intermediate visit status changes (e.g. `waiting` -> `inExamination` when "Call In" is triggered) now broadcast `MessageRoutes.syncVisitUpdated` with full `Patient` and `ClinicVisit` payloads. Ingested state payloads in `_handleSyncFullStateResponse` and the LAN listener now perform status weight conflict resolution (`completed`=3, `inExamination`=2, `waiting`=1), preventing stale node overwrites and firing counter-sync updates back to stale peers.
- **Reception Checkout & Billing State Binding (`ClinicBloc`, `ClinicState` & `ClinicReceptionPage`)**: Fixed Vanishing Patient bug. `ClinicRepositoryImpl.getQueue` now includes `completed` visits, `ClinicBloc` populates the dedicated `billingVisits` state list in `ClinicLoaded`, and `ClinicReceptionPage` directly binds the "Checkout & Billing" tab to `billingVisits`, ensuring completed patient visits seamlessly appear ready for copay collection and receipt printing.
- **Client Auto-Reconnect & Handshake Reconciliation (`LanSyncRepositoryImpl`)**: Fixed Host-Drop Reconnection Flaw. Added an automatic reconnection loop upon server disconnects. On every successful connection and reconnection, the client enforces the 800ms delay and re-transmits `sync.request_active_state` to reconcile any missed state from restarted host servers.
- **Doctor Name Display Translation (`ClinicReceptionPage`)**: Added `formatDoctorName` UI mapper in `ClinicReceptionPage` to translate raw seeded IDs (`usr_doctor`, `usr_doctor_2`, `usr_doctor_tarek`, `usr_doctor_oncall`) into clean human-readable names (`Dr. Sarah Connor`, `Dr. Tarek Dental Lead`, `Dr. On-Call Physician`) with capitalized fallback across Queue and Billing lists.
- **Bulletproof Multi-Ping Handshake (`LanSyncRepositoryImpl`)**: Hardened client state reconciliation with a 3-stage multi-ping handshake (`sync.request_active_state` dispatched at 1s, 3s, and 6s), ensuring slow-booting or initializing Host BLoCs never drop client reconnection sync requests.
- **WhatsApp-Style Offline Outbox Event Queue (`LanSyncRepositoryImpl`)**: Implemented local Outbox Pattern with Hive box `'empos_offline_sync_queue'`. Outgoing broadcast events dispatched while offline or when peers/channels are unavailable are persisted locally to disk instead of dropped. Upon peer reconnection (for clients) or peer join (for hosts), the outbox is automatically flushed in FIFO order before triggering state reconciliation handshake.
- **Outbox Sync Race Condition & Patient Self-Healing (`ClinicBloc`)**: Fixed missing patient race condition when offline actions are flushed. Ingested `visit.updated`, `visitCompleted`, and `patientCheckedIn` events automatically verify and upsert missing patient profile entities into local Hive storage if not yet present on the peer station.
- **Granular UI Rebuild Optimization (`ClinicReceptionPage`)**: Eliminated full-page flashes and jitter during patient intake and queue changes. Removed top-level `BlocBuilder` from the page `Scaffold` and pushed localized builders down to the KPI banner, tab badges, and list views.
- **Checkout & Billing Chronological Sorting (`ClinicBloc` & `ClinicState`)**: Sorted `billingVisits` and `completedQueue` reverse-chronologically by completion/check-in timestamp, ensuring the newest completed visits immediately appear at the top.
- **Queue List Item ValueKey Optimization (`ClinicReceptionPage`)**: Optimized ListView rendering with ValueKeys (`key: ValueKey('queue_visit_${visit.id}')` and `key: ValueKey('billing_visit_${visit.id}')`) and added `buildWhen` filters to eliminate list re-rendering flashes when appending new patients to the queue.
- **Equal-Height KPI Banners (`ClinicReceptionPage`)**: Wrapped reception KPI cards in an `IntrinsicHeight` responsive layout with `Expanded` equal flex distribution, preventing vertical mismatches and text overflow.
- **Contextual Shift Management Controls (`MainShell`)**: Contextually hid the global `OpenShift` header button for `doctor` and `receptionist` roles and on clinic-specific pages, ensuring shift drawer controls only appear for POS Cashiers and Managers.
- **Visit Payment Settlement & Queue Clearing (`ClinicBloc` & `ClinicReceptionPage`)**: Implemented `ProcessVisitPaymentEvent` to mark completed visits as paid (`isPaid: true`), immediately clearing settled visits from the reception billing queue across all LAN nodes with `visit.updated` sync.
- **Patient CRM Linkage & Patient File Lightbox (`ClinicBloc` & `ClinicReceptionPage`)**: Linked clinic check-in directly to `CustomerRepository` so medical patients automatically sync to the CRM Customers directory, and added a "View File" folder action on queue cards to inspect full patient history.
- **Partial Payments & CRM Debt Sync (`PaymentCheckoutDialog` & `ClinicBloc`)**: Added partial payment settlement dialog with real-time remaining debt calculation and automatic updates to `Customer.totalDebt` in `CustomerRepository` with instant CRM BLoC reload.
- **Receptionist Financial Privacy (`CustomersPage`)**: Enforced role-based access control hiding sensitive debtor and aggregate debt amounts from Receptionists.
- **Returning Patient Intake Search (`PatientIntakeDialog`)**: Integrated search and autocomplete by phone number and patient name to quickly lookup existing patient files and prefill intake fields while preserving `patientId`.
- **Dynamic Pediatric vs. Adult Odontogram (`DoctorStationPage`)**: Automatically selects 20-tooth primary dentition for patients under 12 years old and 32-tooth permanent dentition for older patients based on `calculatedAge`.
- **Interactive Clinical Vitals Monitoring (`DoctorStationPage`, `VitalsInputDialog` & `ClinicBloc`)**: Added clickable vitals chips allowing doctors to live-edit Blood Pressure, Heart Rate, SpO2, Temperature, and Respiration with `UpdateVisitVitalsEvent` and LAN synchronization.
- **Patient File 12-Hour AM/PM Time Formatting (`ClinicReceptionPage`)**: Formatted check-in timestamps with `intl` 12-hour AM/PM notation and mapped raw status enums to user-friendly titles.
- **Historical Patient Records Dialog (`DoctorStationPage`)**: Added "View Patient History" action button on Doctor Station vitals dock to open historical consultation records for the active patient (filtering previous visits, chief complaints, diagnoses, prescriptions, and fees).
- **Dental Tooth Matrix Model Subtype Crash (`DentalToothMatrixWidget`)**: Replaced generic `firstWhere(..., orElse: () => ToothChartEntry)` with a type-safe loop in `_findEntry`, preventing covariance subtype runtime exceptions when matching `ToothChartEntryModel` lists.
- **Customer Edit Phone Number & Notes Persistence (`CustomerFormDialog`)**: Ensured customer form dialog properly binds `customer.phone` and saves updated phone, address, and notes to `CustomerRepository`.
- **Patient Intake Phone Number Autofill (`PatientIntakeDialog` & `ClinicBloc`)**: Added `phone` parameter to `CheckInPatientEvent` and `ClinicBloc._onCheckInPatient`, properly populating `phoneController.text` upon selecting a returning patient in `Autocomplete` and persisting contact details to both patient profiles and CRM customer records.
- **Full CRM LAN State Synchronization (`ClinicBloc` & `CustomerBloc`)**: Resolved customer directory desync between Host and Client nodes. Host aggregates all customers in `sync.full_state_response` and clients batch-upsert all received customer models into local storage before dispatching UI events. `CustomerBloc` listens to `customer.updated` and state reconciliation envelopes to keep customer lists and debt balances synchronized in real time without tab switches.
- **Deep Read-Only Medical History Drill-Down (`HistoricalVisitDetailsDialog` & `DoctorStationPage`)**: Made past consultation records interactive in the medical history dialog. Clicking any previous visit opens `HistoricalVisitDetailsDialog` displaying full recorded vitals, chief complaints, clinical diagnoses, prescriptions, executed procedures, financial billing breakdowns, and the exact historical dental odontogram state.
- **Boss & Manager Financial RBAC (`CustomersPage`)**: Enforced role-based access control allowing `admin` and `manager` roles full unmasked access to financial KPI aggregations (`Active Debtors` count and `Total Outstanding Debt`), while masking values as `***` / `Restricted` for restricted roles (`receptionist`, `doctor`).

---

## [1.0.0] - 2026-08-29

### Added
- **Core Architecture**: Full Clean Architecture implementation with strict Domain, Data, and Presentation separation, 100% `StatelessWidget` UI, and BLoC state management.
- **Universal Multi-Industry Engine**: Configurable store blueprint engine covering 11 industry verticals and 41 sub-categories with granular Rule A software/hardware toggles and Rule B typed search indexing.
- **LAN Sync Engine**: Distributed peer-to-peer WebSocket event synchronization hub and client (`LanSyncRepository`) operating on port 9090 with automatic reconnection and structured envelope routing (`MessageRoutes`).
- **Role-Based Access Control (RBAC)**: Secure PIN lock authentication screen (`PinLockScreen`), user session management (`AuthBloc`), and granular UI role guards (`RoleGuardWidget`) supporting Admin, Manager, Cashier, Doctor, Receptionist, and Technician roles.
- **Hardware Peripherals Drivers**: Direct TCP socket driver for ESC/POS thermal receipt printers (port 9100), RJ11 electronic cash drawer kick pulse generation (`0x1B, 0x70, 0x00, 0x19, 0xFA`), and USB HID keyboard-wedge barcode scanner diagnostic feed.
- **Deep Business Engines**:
  - Automated procedure-to-consumables inventory deduction upon dental/medical examination completion.
  - First-Expire, First-Out (FEFO) batch picking algorithm and customer loyalty points accrual (1 pt / 10 currency units).
  - Consolidated Daily Z-Report aggregating shift transactions, cash drawer variance, tax splits, and category revenue breakdowns.
- **Developer RMM Console**: Live remote fleet management console (`DeveloperConsolePage`) with real-time station heartbeats, latency ping diagnostics, remote command dispatch, and OTA software update distribution.
- **Multi-Instance Testing Support**: Environment-based Hive database isolation (`INSTANCE_ID`) enabling concurrent desktop instances without file lock collisions.
- **Testing**: 172 unit, BLoC, and widget test cases with 100% pass rate.
